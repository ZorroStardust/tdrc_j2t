# TDRC-J2T

This project is a demo of transforming joint space to tendon space for a TDRC(Tendon-Driven Robotic Continuum) robot. The code is implemented in both C++ and Python and uses the NumPy library for numerical computations.